import { Pressable, StyleSheet, Text, View } from "react-native";
import { IconSymbol } from "@/components/ui/icon-symbol";
import type { WorkOrder } from "@/lib/field-service/types";
import { StatusPill } from "./status-pill";

function formatTime(value: string) { return new Intl.DateTimeFormat(undefined, { hour: "numeric", minute: "2-digit" }).format(new Date(value)); }
export function WorkOrderRow({ item, onPress }: { item: WorkOrder; onPress: () => void }) { return <Pressable onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.cardPressed]}><View style={styles.topRow}><View style={styles.timeBlock}><IconSymbol name="clock.fill" size={15} color="#64748B" /><Text style={styles.time}>{formatTime(item.scheduledStart)}</Text></View><View style={styles.badges}>{item.priority === "urgent" ? <StatusPill value={item.priority} /> : null}<StatusPill value={item.status} /></View></View><Text style={styles.customer}>{item.customerName}</Text><Text style={styles.site}>{item.serviceSite}</Text><View style={styles.footer}><View style={styles.addressRow}><IconSymbol name="location.fill" size={15} color="#087E8B" /><Text numberOfLines={1} style={styles.address}>{item.address}</Text></View><IconSymbol name="chevron.right" size={18} color="#94A3B8" /></View></Pressable>; }
const styles = StyleSheet.create({ card: { backgroundColor: "#FFFFFF", borderRadius: 16, padding: 16, marginHorizontal: 20, marginBottom: 12, borderWidth: 1, borderColor: "#E6EDF2" }, cardPressed: { opacity: 0.72 }, topRow: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 9 }, timeBlock: { flexDirection: "row", alignItems: "center", gap: 5 }, time: { color: "#64748B", fontSize: 13, fontWeight: "700" }, badges: { flexDirection: "row", gap: 6, alignItems: "center" }, customer: { color: "#123047", fontSize: 17, fontWeight: "800", lineHeight: 22 }, site: { color: "#64748B", fontSize: 14, marginTop: 3, lineHeight: 19 }, footer: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginTop: 14 }, addressRow: { flex: 1, flexDirection: "row", alignItems: "center", gap: 5, marginRight: 8 }, address: { flex: 1, color: "#475569", fontSize: 13 } });
